import styled from 'styled-components';

const Text = styled.p`
  margin-left: 24px;
  color: var(--user-panel-text);
  font-family: var(--main-font);
  font-size: 18px;
  font-style: normal;
  font-weight: 600;
`;

export { Text };
