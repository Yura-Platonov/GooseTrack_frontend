import MonthCalendarHead from '../monthCalendarHead/MonthCalendarHead';

const ChoosedMonth = ({ selectedDate }) => {
  return <MonthCalendarHead selectedDate={selectedDate} />;
};

export default ChoosedMonth;
